# Node modules (prevents unnecessary files from being pushed)
node_modules/

# Environment files (contains secrets like DB credentials, JWT keys)
config.env
.env

# Logs (ignores log files)
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS-specific files
.DS_Store
Thumbs.db

# VS Code settings (ignore workspace settings)
.vscode/

# Dependencies (lock files)
package-lock.json
yarn.lock

# Database (if you use local DB files)
*.sqlite
*.db
